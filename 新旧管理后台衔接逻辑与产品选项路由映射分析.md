# 新旧管理后台衔接逻辑与产品选项路由映射分析

## 一、产品选项（Product Options）的路由映射核对

### 1.1 旧 Admin UI 的路由结构

在旧 Angular 管理后台中，产品选项**没有独立的顶层路由**，它嵌套在产品详情路由下：

**文件**：`packages/admin-ui/src/lib/catalog/src/catalog.routes.ts:73-81`

```typescript
{
    path: 'products/:id/options',         // ← 嵌套在 products 下面
    component: ProductOptionsEditorComponent,
    resolve: createResolveData(ProductVariantsResolver),
    canDeactivate: [CanDeactivateDetailGuard],
    data: { breadcrumb: productOptionsEditorBreadcrumb },
}
```

旧系统的完整路径为：`/admin/catalog/products/:id/options`

这表示在旧系统中：
- 产品选项是**产品详情的子页面**，必须先进入产品详情才能编辑选项
- `ProductOptionsEditorComponent` 同时展示选项组（OptionGroup）和选项值（OptionValue）
- 没有独立的选项组列表页

### 1.2 新 Dashboard 的路由结构

新 React Dashboard 将产品选项重构为**独立的顶层路由**，完全脱离产品详情的嵌套关系：

| 路由文件 | 内部路由 ID | 浏览器路径 | 功能 |
|----------|-------------|-----------|------|
| `option-groups.tsx` | `/_authenticated/_option-groups/option-groups` | `/option-groups` | 选项组列表 |
| `option-groups_.$id.tsx` | `/_authenticated/_option-groups/option-groups_/$id` | `/option-groups/$id` | 选项组详情 |
| `option-groups_.$groupId.options_.$id.tsx` | `/_authenticated/_option-groups/option-groups_/$groupId/options_/$id` | `/option-groups/$groupId/options/$id` | 选项值详情 |

**文件**：`packages/dashboard/src/app/routes/_authenticated/_option-groups/option-groups.tsx:15`

```typescript
export const Route = createFileRoute('/_authenticated/_option-groups/option-groups')({
    component: OptionGroupListPage,
    loader: () => ({ breadcrumb: () => <Trans>Option Groups</Trans> }),
});
```

**文件**：`packages/dashboard/src/app/routes/_authenticated/_option-groups/option-groups_.$groupId.options_.$id.tsx:35-37`

```typescript
export const Route = createFileRoute(
    '/_authenticated/_option-groups/option-groups_/$groupId/options_/$id',
)({...});
```

### 1.3 映射核对结果

| 旧路由 (Angular) | 新路由 (React) | 映射正确性 | 说明 |
|------------------|----------------|-----------|------|
| `/catalog/products/:id/options` | `/option-groups` | ⚠️ **结构性变更** | 旧系统：选项编辑器嵌套在产品详情下；新系统：选项组独立列表 |
| (无) | `/option-groups/$id` | ✅ 新增 | 旧系统无独立选项组详情页 |
| (无) | `/option-groups/$groupId/options/$id` | ✅ 新增 | 旧系统无独立选项值详情页 |
| `/catalog/products/:id` (含选项tab) | `/products/$id` | ⚠️ **功能拆分** | 旧系统中选项组Tab在产品详情内；新系统中产品详情仅保留基本信息 |

**关键发现**：产品选项不是简单的 1:1 路由映射，而是**架构层面的重构**——从"产品子功能"变为"独立实体"。这意味着：

1. 旧系统书签 `/admin/catalog/products/1/options` 在新 Dashboard 中**没有对应路由**
2. 新 Dashboard 中选项组通过面包屑支持从产品详情页导航过来（`?from=product&productId=1`）
3. `ProductOptionsTable` 组件中 `getOptionHref` 构建了正确的链接路径

**文件**：`packages/dashboard/src/app/routes/_authenticated/_option-groups/option-groups_.$id.tsx:202-205`

```typescript
<ProductOptionsTable
    productOptionGroupId={entity.id}
    getOptionHref={optionId => `/option-groups/${entity.id}/options/${optionId}`}
    newOptionHref={`/option-groups/${entity.id}/options/new`}
    linkSearch={search.from === 'product' ? search : undefined}
/>
```

### 1.4 默认菜单中的选项组配置

**文件**：`packages/dashboard/src/lib/framework/defaults.ts:43-49`

```typescript
{
    id: 'option-groups',
    title: 'Option Groups',
    url: '/option-groups',
    order: 250,                                    // ← 在 Product Variants(200) 和 Facets(300) 之间
    requiresPermission: ['ReadProduct', 'ReadCatalog'],
}
```

这在旧系统中没有对应的菜单项（旧系统中选项编辑只能通过产品详情页进入）。

---

## 二、basepath 条件下菜单链接跳转到旧后台的具体行为

### 2.1 两套系统的 basepath 机制

#### 新 Dashboard 的 basepath 处理链路

整个 basepath 处理涉及**三层协同**：

**第一层：Vite 构建配置**

Vite 的 `base` 配置（通常为 `/dashboard/`）决定静态资源的 URL 前缀。但 `vite-plugin-transform-index.ts` 会在构建产物中：
1. 移除 HTML 中 `href`/`src` 的 base 前缀（避免浏览器请求 `/dashboard/assets/xxx` 被服务端误处理）
2. 插入 `<base href="/dashboard/">` 标签，让浏览器基于此解析相对路径

**文件**：`packages/dashboard/vite/vite-plugin-transform-index.ts:31-41`

```typescript
if (config.base && config.base !== '/') {
    const basePath = config.base.replace(/\/$/, '');
    // 移除 href/src 中的 base 前缀
    const attributeRegex = new RegExp(`(href|src)=(["'])${basePath}/?`, 'g');
    let transformedHtml = html.replace(attributeRegex, '$1=$2');
    // 在 <head> 中插入 <base> 标签
    const baseTag = `        <base href="${config.base}">\n`;
    transformedHtml = transformedHtml.replace(/<head>/, `<head>\n${baseTag}`);
    return transformedHtml;
}
```

**第二层：TanStack Router 的 basepath**

**文件**：`packages/dashboard/src/app/main.tsx:29-40`

```typescript
const processedBaseUrl = (() => {
    const baseUrl = import.meta.env.BASE_URL;        // ← 来自 Vite 的 base 配置
    if (!baseUrl || baseUrl === '/') return undefined;
    const normalized = baseUrl.startsWith('/') ? baseUrl : '/' + baseUrl;
    return normalized.endsWith('/') ? normalized.slice(0, -1) : normalized;
})();

const routerOptions = {
    basepath: processedBaseUrl,                       // ← e.g. '/dashboard'
    // ...
};
```

**第三层：NavMain 中的路径比较**

**文件**：`packages/dashboard/src/lib/components/layout/nav-main.tsx:101-126`

```typescript
const basePath = router.basepath || '';               // ← '/dashboard'

const isPathActive = React.useCallback(
    (itemUrl: string) => {
        // 移除 basepath 前缀后再比较
        const normalizedCurrentPath = basePath
            ? currentPath.replace(new RegExp(`^${escapeRegexChars(basePath)}`), '')
            : currentPath;
        const cleanPath = normalizedCurrentPath.startsWith('/')
            ? normalizedCurrentPath
            : `/${normalizedCurrentPath}`;
        if (itemUrl === '/') {
            return cleanPath === '/' || cleanPath === '';
        }
        return cleanPath === itemUrl || cleanPath.startsWith(`${itemUrl}/`);
    },
    [currentPath, basePath],
);
```

#### 旧 Admin UI 的 basepath 处理

**文件**：`packages/admin-ui-plugin/src/plugin.ts:366-384`

```typescript
private async overwriteBaseHref(indexHtmlPath: string, baseHref: string) {
    const withCustomBaseHref = indexHtmlContent.replace(
        /<base href=".+"\s*\/>/,
        `<base href="/${baseHref}/" />`,               // ← e.g. <base href="/admin/">
    );
    await fs.writeFile(indexHtmlPath, withCustomBaseHref);
}
```

### 2.2 basepath 下的跳转行为分析

假设配置如下：
- AdminUiPlugin: `route: 'admin'` → 旧系统路径前缀 `/admin/`
- DashboardPlugin: `route: 'dashboard'` → 新系统路径前缀 `/dashboard/`

#### 场景一：从新 Dashboard 菜单点击指向旧后台的链接

当 `navSections` 中配置了 `url: '/admin/catalog/products'` 时：

**步骤分解**：

1. `NavMain` 组件渲染 `<SidebarMenuSubButton render={<Link to="/admin/catalog/products" />}>` 
2. TanStack Router 尝试在**内部路由树**中匹配 `/admin/catalog/products`
3. 由于 basepath 为 `/dashboard`，实际匹配的路径为 `/admin/catalog/products`（不含 basepath 前缀）
4. **路由树中不存在此路径** → TanStack Router 抛出路由未找到错误
5. `defaultErrorComponent` 渲染错误信息：`An error occurred: ...`

**结论**：直接在 `url` 字段使用旧后台路径**不可行**，`<Link to={...}>` 只能匹配内部路由。

#### 场景二：使用 `<a href>` 替代 `<Link to>`

如果改造 `NavMain` 组件，对外部链接使用原生 `<a>` 标签：

1. `<a href="/admin/catalog/products">` 被渲染
2. 浏览器的 `<base href="/dashboard/">` 生效，将相对路径解析为 `http://host/dashboard/admin/catalog/products`
3. **这是错误的！** 因为旧后台的实际路径是 `/admin/catalog/products`，不是 `/dashboard/admin/...`

**解决方案**：必须使用**绝对 URL** 跳转：

```typescript
// 方案 A：使用完整的绝对路径
<a href={`http://${window.location.host}/admin/catalog/products`}>

// 方案 B：使用 window.location.origin 拼接
<a href={`${window.location.origin}/admin/catalog/products`}>

// 方案 C：使用 JavaScript 跳转（绕过 <base> 标签影响）
<button onClick={() => { window.location.href = '/admin/catalog/products'; }}>
```

**方案 C 最可靠**，因为 `window.location.href = '/admin/...'` 直接让浏览器导航到绝对路径，不受 `<base>` 标签影响。

#### 场景三：basepath 为 `/`（默认配置）

当 Vite `base` 为默认值 `/` 时：
- `processedBaseUrl` 为 `undefined`
- TanStack Router 的 `basepath` 为 `undefined`
- HTML 中没有 `<base>` 标签
- 此时 `<a href="/admin/catalog/products">` 可以正确跳转

### 2.3 basepath 影响总结

| 配置 | `<Link to="/admin/...">` | `<a href="/admin/...">` | `window.location.href` |
|------|--------------------------|-------------------------|------------------------|
| basepath = `/` | ❌ 路由未找到 | ✅ 正确跳转 | ✅ 正确跳转 |
| basepath = `/dashboard` | ❌ 路由未找到 | ❌ 被解析为 `/dashboard/admin/...` | ✅ 正确跳转 |

---

## 三、NavMain 中 SidebarMenuButton 的实际渲染逻辑

### 3.1 三种渲染路径

`nav-main.tsx` 中存在三种渲染路径，**全部使用 TanStack Router 的 `<Link>`**：

#### 路径一：独立菜单项（无子项，如 Insights）

**文件**：`nav-main.tsx:253-267`

```typescript
if ('url' in item) {
    return (
        <SidebarMenuButton
            tooltip={i18n.t(item.title)}
            render={<Link to={item.url} />}           // ← TanStack Router Link
            isActive={isPathActive(item.url)}
        >
            {item.icon && <item.icon />}
            <span>{i18n.t(item.title)}</span>
        </SidebarMenuButton>
    );
}
```

#### 路径二：折叠状态的分区菜单

**文件**：`nav-main.tsx:56-91`

```typescript
// HoverCard 中也使用 <Link>
<Link to={subItem.url} className={cn(...)}>
    {i18n.t(subItem.title)}
</Link>
```

#### 路径三：展开状态的分区子菜单

**文件**：`nav-main.tsx:295-310`

```typescript
<SidebarMenuSubButton
    render={<Link to={subItem.url} />}               // ← TanStack Router Link
    isActive={isPathActive(subItem.url)}
>
    <span>{i18n.t(subItem.title)}</span>
</SidebarMenuSubButton>
```

### 3.2 外部链接处理缺陷

当前代码**没有区分内部路由和外部链接**。所有菜单项的 URL 都通过 `<Link to={item.url}>` 渲染。这意味着：

- 如果 `item.url` 不在路由树中 → TanStack Router 抛出错误
- 如果 `item.url` 以 `/admin` 开头 → 不在新 Dashboard 路由树中 → 必然出错
- 没有任何 `try/catch` 或 `onError` 处理来优雅降级

### 3.3 修复方案

需要在 `renderSection` 函数中添加外部链接判断逻辑：

```typescript
// 判断是否为外部链接（指向旧管理后台或其他系统）
const isExternalUrl = (url: string) => {
    if (url.startsWith('http://') || url.startsWith('https://')) return true;
    // 检查 URL 是否不在当前 basepath 下
    const base = router.basepath || '';
    const effectiveBase = base ? base : '';
    // 如果 URL 以其他已知前缀开头（如 /admin），视为外部链接
    if (effectiveBase && url.startsWith('/') && !url.startsWith(effectiveBase + '/') && url !== effectiveBase) {
        return true;
    }
    return false;
};

// 在渲染时区分处理
if ('url' in item) {
    if (isExternalUrl(item.url)) {
        return (
            <SidebarMenuButton
                tooltip={i18n.t(item.title)}
                onClick={() => { window.location.href = item.url; }}
            >
                {item.icon && <item.icon />}
                <span>{i18n.t(item.title)}</span>
            </SidebarMenuButton>
        );
    }
    // 正常内部路由
    return (
        <SidebarMenuButton
            tooltip={i18n.t(item.title)}
            render={<Link to={item.url} />}
            isActive={isPathActive(item.url)}
        >
            ...
        </SidebarMenuButton>
    );
}
```

---

## 四、DashboardPlugin 与 AdminUiPlugin 的 route/baseHref 协同

### 4.1 服务端路由注册

两个插件通过 NestJS `MiddlewareConsumer` 各自注册 Express 中间件：

**DashboardPlugin**（`packages/dashboard/plugin/dashboard.plugin.ts:177-184`）：

```typescript
const { route, appDir, viteDevServerPort = 5173 } = DashboardPlugin.options;
consumer.apply(this.createDynamicHandler(route, appDir, viteDevServerPort)).forRoutes(route);
registerPluginStartupMessage('Dashboard UI', route);   // e.g. "Dashboard UI: /dashboard"
```

**AdminUiPlugin**（`packages/admin-ui-plugin/src/plugin.ts:184-259`）：

```typescript
const { route } = AdminUiPlugin.options;               // e.g. "admin"
consumer.apply(this.createStaticServer(app)).forRoutes(route);
registerPluginStartupMessage('Admin UI', route);        // e.g. "Admin UI: /admin"
```

### 4.2 请求路由分发

两个插件注册在不同的 Express 路由上：

```
浏览器请求
  ├── /dashboard/*     → DashboardPlugin 的 DynamicHandler
  │                        ├── Vite Dev Server 代理 (开发模式)
  │                        ├── 静态文件服务 (生产模式)
  │                        └── 默认引导页面 (无构建文件)
  │
  ├── /admin/*         → AdminUiPlugin 的 StaticServer
  │                        └── SPA 回退 → index.html
  │
  ├── /admin-api/*     → Vendure Admin GraphQL API
  │
  └── /shop-api/*      → Vendure Shop GraphQL API
```

### 4.3 SPA 回退行为

两个插件都使用 SPA 回退模式——所有未匹配的子路径都返回各自的 `index.html`：

**DashboardPlugin**（`dashboard.plugin.ts:216-218`）：

```typescript
dashboardServer.use((req, res) => {
    res.sendFile('index.html', { root: dashboardPath });
});
```

**AdminUiPlugin**（`plugin.ts:278-280`）：

```typescript
adminUiServer.use((req, res) => {
    res.sendFile('index.html', { root: adminUiAppPath });
});
```

这意味着：
- 访问 `/dashboard/option-groups/1` → 返回 Dashboard 的 `index.html` → TanStack Router 处理 `/option-groups/1`
- 访问 `/admin/catalog/products/1` → 返回 Admin UI 的 `index.html` → Angular Router 处理 `/catalog/products/1`
- 访问 `/dashboard/admin/...` → 返回 Dashboard 的 `index.html` → TanStack Router 找不到路由 → 404

### 4.4 basepath 传递链路图

```
Vite 配置 (base: '/dashboard/')
  │
  ├──→ vite-plugin-transform-index.ts
  │      ├── 移除 HTML 中 href/src 的 /dashboard/ 前缀
  │      └── 插入 <base href="/dashboard/">
  │
  ├──→ import.meta.env.BASE_URL = '/dashboard/'
  │      └──→ main.tsx processedBaseUrl = '/dashboard'
  │              └──→ routerOptions.basepath = '/dashboard'
  │                      ├──→ TanStack Router 路由匹配基准
  │                      └──→ NavMain isPathActive 路径比较基准
  │
  └──→ 静态资源 URL 前缀 (e.g. /dashboard/assets/xxx.js)
```

---

## 五、核对矩阵

### 5.1 跳转触发条件核对矩阵

| 编号 | 触发条件 | 入口位置 | 权限归属（跳转前） | 权限归属（跳转后） | 验证步骤 |
|------|----------|----------|-------------------|-------------------|----------|
| T1 | 用户点击新 Dashboard 菜单中指向旧后台的链接 | `NavMain` → `SidebarMenuSubButton` | 新 Dashboard: `NavMenuItem.requiresPermission` → `usePermissions().hasPermissions()` | 旧 Admin UI: `AuthGuard.canActivate()` → `PermissionsService.userHasPermissions()` | 1. 在 `navSections` 中配置 `url: '/admin/...'` 的菜单项<br>2. 以无 `ReadCatalog` 权限的用户登录<br>3. 确认菜单项不可见<br>4. 以有权限用户登录后点击链接<br>5. 确认跳转到旧后台且菜单正常显示 |
| T2 | 用户在新 Dashboard 中直接输入旧后台 URL | 浏览器地址栏 | 无（浏览器直接导航） | 旧 Admin UI: `AuthGuard.canActivate()` 检查认证状态 | 1. 在新 Dashboard 中认证<br>2. 地址栏输入 `http://host/admin/catalog/products`<br>3. 确认旧后台无需重新登录<br>4. 确认旧后台菜单根据权限过滤 |
| T3 | 用户在旧后台中直接输入新 Dashboard URL | 浏览器地址栏 | 无（浏览器直接导航） | 新 Dashboard: `_authenticated.beforeLoad()` 检查 `context.auth.isAuthenticated` | 1. 在旧后台中认证<br>2. 地址栏输入 `http://host/dashboard/products`<br>3. 确认新 Dashboard 无需重新登录<br>4. 确认菜单根据权限过滤 |
| T4 | 用户点击产品详情中的选项组链接 | `ProductOptionsTable` → `getOptionHref` | 新 Dashboard: 页面级权限（通过 `_authenticated` 路由守卫） | 同一 SPA 内导航，无需额外权限校验 | 1. 打开产品详情 `/products/1`<br>2. 滚动到 Option Groups 区域<br>3. 点击选项组名称<br>4. 确认导航到 `/option-groups/1`<br>5. 确认面包屑正确显示来源 |
| T5 | 用户从选项组详情导航到选项值详情 | `ProductOptionsTable` → `newOptionHref` / `getOptionHref` | 新 Dashboard: 页面级权限 | 同一 SPA 内导航 | 1. 打开选项组详情 `/option-groups/1`<br>2. 点击 Product Options 表格中的选项值<br>3. 确认导航到 `/option-groups/1/options/2`<br>4. 确认选项值详情页正确展示 |
| T6 | basepath 非默认时点击旧后台链接 | `NavMain` 组件（需改造） | 新 Dashboard: 菜单权限过滤 | 旧 Admin UI: `AuthGuard` + `PermissionsService` | 1. 设置 Vite `base: '/dashboard/'`<br>2. 配置指向旧后台的菜单项<br>3. 使用 `window.location.href` 方式跳转<br>4. 确认不被 `<base>` 标签干扰<br>5. 确认到达正确的旧后台页面 |
| T7 | 扩展路由中注册指向旧后台的路由 | `defineDashboardExtension({ routes: [...] })` | 新 Dashboard: `navMenuItem.requiresPermission` | 旧 Admin UI: `AuthGuard` + `PermissionsService` | 1. 通过 `defineDashboardExtension` 注册路由<br>2. 配置 `component` 为跳转中间页<br>3. 点击菜单项<br>4. 确认中间页正确跳转到旧后台 |
| T8 | 未认证用户访问任何认证路由 | 浏览器地址栏 / 深度链接 | 新 Dashboard: `_authenticated.beforeLoad()` → `redirect('/login')` | 旧 Admin UI: `AuthGuard` → `router.navigate(['/login'])` 或 `window.location.href = externalLoginUrl` | 1. 未登录状态访问 `/dashboard/products`<br>2. 确认重定向到 `/login`<br>3. 登录后确认重定向回原始页面 |
| T9 | 用户切换 Channel 后跳转到旧后台 | Channel 切换按钮 + 菜单链接 | 新 Dashboard: 菜单权限 + Channel 级权限 | 旧 Admin UI: `PermissionsService.setCurrentUserPermissions()` 在 channel 切换时重新设置 | 1. 在新 Dashboard 中切换到 Channel B<br>2. 点击跳转到旧后台<br>3. 确认旧后台的 activeChannel 是否同步<br>4. 确认菜单权限按 Channel B 过滤 |

### 5.2 产品选项路由映射核对矩阵

| 旧路由 | 新路由 | 功能描述 | 映射类型 | 验证步骤 |
|--------|--------|----------|----------|----------|
| `/admin/catalog/products/:id/options` | `/option-groups` | 选项组列表 | 结构变更：嵌套→独立 | 1. 旧系统：进入产品详情→选项Tab<br>2. 新系统：直接从菜单进入选项组列表<br>3. 确认数据一致性 |
| (无) | `/option-groups/$id` | 选项组详情 | 新增路由 | 1. 从选项组列表点击进入详情<br>2. 确认选项值列表正确展示<br>3. 确认编辑/保存功能正常 |
| (无) | `/option-groups/$groupId/options/$id` | 选项值详情 | 新增路由 | 1. 从选项组详情点击选项值<br>2. 确认选项值名称、code 可编辑<br>3. 确认保存后返回选项组详情 |
| `/admin/catalog/products/:id` (含选项Tab) | `/products/$id` (不含选项Tab) | 产品详情 | 功能拆分 | 1. 旧系统：产品详情含选项组Tab<br>2. 新系统：产品详情仅含基本信息<br>3. 确认选项组通过独立路由管理 |
| (无面包屑) | `/option-groups/$id?from=product&productId=1` | 从产品导航到选项组 | 新增面包屑 | 1. 在产品详情中点击选项组链接<br>2. 确认面包屑: Products > {产品名} > Option Groups > {选项组名}<br>3. 确认返回导航正确 |

### 5.3 basepath 条件下链接行为核对矩阵

| 场景 | basepath 配置 | 链接方式 | 预期行为 | 实际行为 | 是否需要修复 |
|------|--------------|----------|----------|----------|-------------|
| 内部路由导航 | `/` | `<Link to="/products">` | 正常导航 | ✅ 正常 | 否 |
| 内部路由导航 | `/dashboard` | `<Link to="/products">` | 正常导航（TanStack Router 自动添加 basepath） | ✅ 正常 | 否 |
| 跳转到旧后台 | `/` | `<Link to="/admin/catalog/products">` | 路由未找到错误 | ❌ 报错 | 是 |
| 跳转到旧后台 | `/dashboard` | `<Link to="/admin/catalog/products">` | 路由未找到错误 | ❌ 报错 | 是 |
| 跳转到旧后台 | `/` | `<a href="/admin/catalog/products">` | 正确跳转 | ✅ 正常 | 否 |
| 跳转到旧后台 | `/dashboard` | `<a href="/admin/catalog/products">` | 正确跳转 | ❌ 被解析为 `/dashboard/admin/...` | 是 |
| 跳转到旧后台 | `/` | `window.location.href = '/admin/...'` | 正确跳转 | ✅ 正常 | 否 |
| 跳转到旧后台 | `/dashboard` | `window.location.href = '/admin/...'` | 正确跳转 | ✅ 正常 | 否 |
| 跳转到旧后台 | `/dashboard` | `<a href="http://host/admin/...">` | 正确跳转 | ✅ 正常 | 否 |

### 5.4 权限校验责任归属核对矩阵

| 校验层级 | 旧 Admin UI | 新 Dashboard | 共同后端 | 验证步骤 |
|----------|-------------|-------------|----------|----------|
| 路由级认证 | `AuthGuard.canActivate()` → `AuthService.checkAuthenticatedStatus()` | `_authenticated.beforeLoad()` → `context.auth.isAuthenticated` | — | 1. 未登录访问受保护路由<br>2. 确认两系统均重定向到登录页 |
| 菜单可见性 | `NavMenuItem.requiresPermission` (string \| function) | `NavMenuItem.requiresPermission` (string \| string[]) | — | 1. 无 `ReadCatalog` 权限用户登录<br>2. 确认两系统均隐藏 Catalog 菜单 |
| 组件级权限 | 模板中 `*vdrIfPermissions` 指令 | `<PermissionGuard requires={[...]}>` | — | 1. 无 `UpdateProduct` 权限用户登录<br>2. 确认两系统均隐藏保存按钮 |
| API 级权限 | — | — | GraphQL `@hasPermissions` 指令 + Channel 级过滤 | 1. 直接调用 API 无权限操作<br>2. 确认后端返回 FORBIDDEN |
| Channel 隔离 | `PermissionsService.setCurrentUserPermissions()` 在 channel 切换时重设 | `useChannel().activeChannel` + `usePermissions().hasPermissions()` | 服务端根据 `vendure-token` header 过滤 | 1. 切换到 Channel B<br>2. 确认菜单和数据按 Channel B 权限过滤 |

---

## 六、关键文件索引

| 分析维度 | 文件路径 | 关键行号 |
|----------|----------|----------|
| 产品选项旧路由 | `packages/admin-ui/src/lib/catalog/src/catalog.routes.ts` | 73-81 |
| 选项组列表新路由 | `packages/dashboard/src/app/routes/_authenticated/_option-groups/option-groups.tsx` | 15-18 |
| 选项组详情新路由 | `packages/dashboard/src/app/routes/_authenticated/_option-groups/option-groups_.$id.tsx` | 42-87, 202-205 |
| 选项值详情新路由 | `packages/dashboard/src/app/routes/_authenticated/_option-groups/option-groups_.$groupId.options_.$id.tsx` | 35-37 |
| 选项组默认菜单配置 | `packages/dashboard/src/lib/framework/defaults.ts` | 43-49 |
| basepath 处理（Vite 插件） | `packages/dashboard/vite/vite-plugin-transform-index.ts` | 31-41 |
| basepath 处理（main.tsx） | `packages/dashboard/src/app/main.tsx` | 29-40 |
| basepath 处理（NavMain） | `packages/dashboard/src/lib/components/layout/nav-main.tsx` | 101-126 |
| NavMain 渲染逻辑 | `packages/dashboard/src/lib/components/layout/nav-main.tsx` | 248-318 |
| 认证路由守卫 | `packages/dashboard/src/app/routes/_authenticated.tsx` | 7-22 |
| 旧 Admin UI 路由守卫 | `packages/admin-ui/src/lib/core/src/providers/guard/auth.guard.ts` | 16-36 |
| 旧 Admin UI baseHref 写入 | `packages/admin-ui-plugin/src/plugin.ts` | 366-384 |
| DashboardPlugin 路由注册 | `packages/dashboard/plugin/dashboard.plugin.ts` | 166-184, 287-395 |
| AdminUiPlugin 路由注册 | `packages/admin-ui-plugin/src/plugin.ts` | 173-260 |
| 扩展路由合并 | `packages/dashboard/src/lib/framework/page/use-extended-router.tsx` | 12-132 |

---

**报告生成时间**：2026-05-31  
**分析代码版本**：Vendure v3.x  
**分析范围**：packages/admin-ui-plugin、packages/admin-ui、packages/dashboard 核心模块
