# 未迁移视图备用路径与新旧路由衔接深度分析

## 一、核心结论

经过对 `packages/admin-ui-plugin`、`packages/admin-ui`、`packages/dashboard` 三个包的完整代码审查，得出以下关键结论：

1. **新 Dashboard 中没有内置任何跳转至旧管理后台的逻辑**——两套系统完全独立运行，不存在自动回退机制
2. **旧管理后台已标记为弃用（Deprecated）**，计划于 2026 年 7 月停止维护
3. **两套系统共享同一套后端权限模型**，但前端权限校验的实现完全独立
4. **跳转入口需开发者手动通过扩展 API 注册**

---

## 二、跳转至旧管理后台的触发条件分析

### 2.1 当前代码中无自动跳转

在 `packages/dashboard/src` 整个目录中搜索 `admin`、`legacy`、`redirect`、`fallback` 等关键词，**没有发现任何自动跳转到旧管理后台的代码逻辑**。这意味着：

- 新 Dashboard 访问不存在的路由时，只会显示 TanStack Router 的默认 404 或 `ErrorPage`
- 不会自动回退到旧 Admin UI 对应页面
- 两套 SPA 各自独立处理客户端路由

### 2.2 手动跳转触发场景

需要开发者自行处理的跳转场景包括：

| 场景 | 触发方式 | 实现位置 |
|------|----------|----------|
| 功能未迁移 | 用户点击导航菜单项 | 新 Dashboard 的 `navSections` 扩展 |
| 外部链接跳转 | 用户点击旧系统链接 | `window.open()` 或 `<a>` 标签 |
| 深度链接回退 | 用户书签/历史记录指向旧路径 | 服务端 Nginx/CDN 重写规则 |
| 插件扩展未适配 | 第三方插件仅注册了 Angular 路由 | 需同时注册新旧两套路由 |

### 2.3 关键代码证据

**旧 Admin UI 的弃用声明**（`packages/admin-ui-plugin/src/plugin.ts:95-99`）：

```typescript
/**
 * :::warning[Deprecated]
 * From Vendure v3.5.0, the Angular-based Admin UI has been replaced by the new
 * React Admin Dashboard. The Angular Admin UI will not be maintained after
 * **July 2026**. Until then, we will continue patching critical bugs and
 * security issues.
 * :::
 */
```

**旧 Admin UI 的 `compatibilityMode` 选项已废弃**（`packages/admin-ui-plugin/src/plugin.ts:84-89`）：

```typescript
/**
 * @deprecated This option no longer has any effect.
 * Previously used when running the AdminUiPlugin at the same time as the new
 * DashboardPlugin to avoid conflicts, but this is no longer necessary as the
 * schemas use different type names.
 */
compatibilityMode?: boolean;
```

这表明两套系统的 GraphQL schema 已经使用不同的类型名称，不再有冲突问题。

---

## 三、如何将跳转入口关联到新菜单

### 3.1 方案一：通过 navSections 函数修改器注入跳转分区

最推荐的方式，使用函数形式的 `navSections`，在新 Dashboard 侧边栏底部添加"旧管理后台"分区：

```typescript
import { defineDashboardExtension } from '@vendure/dashboard';

defineDashboardExtension({
    navSections: (config) => ({
        sections: [
            ...config.sections,
            {
                id: 'legacy-admin',
                title: 'Legacy Admin UI',
                placement: 'bottom',
                order: 999,
                items: [
                    {
                        id: 'legacy-dashboard',
                        title: 'Dashboard (Legacy)',
                        url: '/admin/dashboard',
                        requiresPermission: ['Authenticated'],
                    },
                    {
                        id: 'legacy-products',
                        title: 'Products (Legacy)',
                        url: '/admin/catalog/products',
                        requiresPermission: ['ReadCatalog'],
                    },
                    {
                        id: 'legacy-orders',
                        title: 'Orders (Legacy)',
                        url: '/admin/orders',
                        requiresPermission: ['ReadOrder'],
                    },
                    // ... 添加其他未迁移功能的跳转链接
                ],
            },
        ],
    }),
});
```

**关键实现细节**：

- `placement: 'bottom'` 确保分区出现在侧边栏底部（与 System/Settings 同区）
- `url` 使用绝对路径 `/admin/...` 指向旧管理后台
- 由于旧后台和新的 Dashboard 部署在同一域名下（不同路径前缀），点击链接会触发**整页导航**到旧 SPA
- `requiresPermission` 字段确保权限不足的用户看不到跳转入口

### 3.2 方案二：通过 routes 注册自定义跳转组件

适合需要在新 Dashboard 布局内嵌入跳转链接（而非整页导航）的场景：

```typescript
defineDashboardExtension({
    routes: [
        {
            path: '/legacy-redirect',
            component: (route) => {
                // 渲染一个带跳转提示的中间页
                return <LegacyRedirectPage target="/admin/catalog/products" />;
            },
            navMenuItem: {
                sectionId: 'catalog',
                title: 'Products (Legacy)',
                requiresPermission: ['ReadCatalog'],
            },
        },
    ],
});
```

### 3.3 方案三：服务端重写规则

在 Nginx/CDN 层配置重写规则，将新 Dashboard 中未实现的路由重定向到旧管理后台：

```nginx
# Nginx 示例：将 /dashboard/inventory 重写到旧管理后台
location /dashboard/inventory {
    return 301 /admin/catalog/inventory;
}
```

### 3.4 菜单跳转的行为差异

| 方式 | 导航行为 | 会话保持 | 用户体验 |
|------|----------|----------|----------|
| 整页导航（`<a href="/admin/...">`) | 离开新 SPA，加载旧 SPA | 共享 cookie/token | 有明显切换感 |
| 侧边栏 `url` 属性 | TanStack Router 尝试匹配内部路由 | — | 未匹配时显示 404 |
| `window.open` | 新标签页打开旧后台 | 共享 cookie/token | 可同时使用两套系统 |
| 服务端 301 重写 | 浏览器地址栏变更 | 共享 cookie/token | 透明跳转 |

**重要提示**：新 Dashboard 的 `NavMenuItem.url` 属性会被 `Link` 组件使用。如果 `url` 指向 `/admin/...`，TanStack Router 会尝试在内部路由树中匹配。由于旧管理后台的路由不在新路由树中，**必须在 `NavMain` 组件层面特殊处理外部链接**。

当前 `nav-main.tsx` 中的实现（`packages/dashboard/src/lib/components/layout/nav-main.tsx:254-267`）：

```typescript
<SidebarMenuButton
    tooltip={i18n.t(item.title)}
    render={<Link to={item.url} />}  // ← 这里使用 TanStack Router 的 Link
    isActive={isPathActive(item.url)}
>
```

**要支持外部跳转链接，需改造渲染逻辑**：

```typescript
// 改造方案：区分内部路由和外部链接
const isExternalLink = item.url.startsWith('/admin') || item.url.startsWith('http');

if (isExternalLink) {
    return (
        <SidebarMenuButton
            tooltip={i18n.t(item.title)}
            onClick={() => window.open(item.url, '_blank')}
        >
            {item.icon && <item.icon />}
            <span>{i18n.t(item.title)}</span>
        </SidebarMenuButton>
    );
}

return (
    <SidebarMenuButton
        tooltip={i18n.t(item.title)}
        render={<Link to={item.url} />}
        isActive={isPathActive(item.url)}
    >
        ...
    </SidebarMenuButton>
);
```

---

## 四、权限校验在跳转前后的责任归属

### 4.1 权限模型对比

两套系统共享同一个后端 GraphQL Admin API，因此**权限定义完全一致**，但前端校验实现不同：

| 维度 | 旧 Admin UI (Angular) | 新 Dashboard (React) |
|------|----------------------|---------------------|
| 权限来源 | `me.channels[].permissions` | `me.channels[].permissions` |
| GraphQL 查询 | `CurrentUserQuery` (Angular Apollo) | `CurrentUserQuery` (React Query) |
| 权限存储 | `PermissionsService.currentUserPermissions` | `AuthContext.channels` + `useChannel` |
| 菜单权限字段 | `requiresPermission: string \| ((perms) => boolean)` | `requiresPermission: string \| string[]` |
| 校验逻辑 | `some()` — 任一权限匹配即通过 | `some()` — 任一权限匹配即通过 |
| 守卫组件 | `AuthGuard` (canActivate) | `PermissionGuard` (组件级) |
| 路由级守卫 | Angular `canActivate` 路由守卫 | TanStack Router `beforeLoad` 重定向 |

### 4.2 权限校验流程对比

#### 旧 Admin UI 权限流程

**文件位置**：`packages/admin-ui/src/lib/core/src/providers/guard/auth.guard.ts:16-36`

```
用户请求路由
  → AuthGuard.canActivate()
    → AuthService.checkAuthenticatedStatus()
      → 未认证 → 重定向到 /login 或外部 loginUrl
      → 已认证 → 继续
        → PermissionsService.setCurrentUserPermissions()
          → NavMenuItem.requiresPermission 过滤
          → 组件级 requiresPermission 检查
```

#### 新 Dashboard 权限流程

**文件位置**：
- 认证路由：`packages/dashboard/src/app/routes/_authenticated.tsx:7-22`
- 权限 Hook：`packages/dashboard/src/lib/hooks/use-permissions.ts:23-47`
- 权限守卫：`packages/dashboard/src/lib/components/shared/permission-guard.tsx:44-51`

```
用户请求路由
  → _authenticated.beforeLoad()
    → context.auth.isAuthenticated?
      → 否 → redirect('/login')
      → 是 → 渲染 <AppLayout>
        → <AppSidebar> → NavMain → isItemAllowed() 过滤菜单
          → usePermissions().hasPermissions()
            → AuthContext.channels → activeChannel.permissions
        → <PermissionGuard> 组件级权限检查
          → 无权限 → 渲染 null
          → 有权限 → 渲染 children
```

### 4.3 跳转前后的权限责任矩阵

| 阶段 | 责任方 | 校验内容 | 代码位置 |
|------|--------|----------|----------|
| 跳转前（新 Dashboard） | 新 Dashboard | `NavMenuItem.requiresPermission` 过滤菜单可见性 | `nav-main.tsx:164-175` |
| 跳转前（新 Dashboard） | 新 Dashboard | `PermissionGuard` 保护 UI 元素 | `permission-guard.tsx:44-51` |
| 跳转前（新 Dashboard） | 新 Dashboard | `beforeLoad` 路由守卫检查认证状态 | `_authenticated.tsx:8-16` |
| 跳转后（旧 Admin UI） | 旧 Admin UI | `AuthGuard.canActivate()` 检查认证 | `auth.guard.ts:23-35` |
| 跳转后（旧 Admin UI） | 旧 Admin UI | `PermissionsService.userHasPermissions()` 过滤菜单 | `permissions.service.ts:33-40` |
| 跳转后（旧 Admin UI） | 旧 Admin UI | `NavMenuItem.requiresPermission` 过滤菜单项 | `nav-builder-types.ts:46` |
| 跳转后（后端） | GraphQL Admin API | 每个 mutation/query 的 `@hasPermissions` 指令 | 服务端解析器 |
| 跳转后（后端） | GraphQL Admin API | Channel 级权限隔离 | 服务端中间件 |

### 4.4 关键风险点

1. **Cookie/Token 共享**：两套系统默认共享同一域名下的认证 cookie 或 bearer token，因此跳转后**无需重新登录**
2. **Channel 切换不同步**：新 Dashboard 中切换 Channel 后跳转到旧管理后台，旧系统的 activeChannel 可能不同步
3. **权限声明不一致**：旧 Admin UI 的 `requiresPermission` 支持函数形式 `(userPermissions: string[]) => boolean`，新 Dashboard 仅支持字符串数组。跨系统跳转时需确保权限声明一致
4. **前端权限校验非最终防线**：无论哪套系统，前端权限仅控制 UI 可见性，**后端 API 才是权限的最终执行者**

---

## 五、新旧路由核对清单

### 5.1 路由命名规范对照

| 规范 | 旧 Admin UI (Angular) | 新 Dashboard (React) |
|------|----------------------|---------------------|
| 列表页 | `products` | `products` |
| 详情页 | `products/:id` | `products/$id` |
| 子资源详情 | `products/:productId/variants/:id` | `products/$id/variants` |
| 草稿订单 | `draft/:id` | `orders/draft/$id` |
| 修改订单 | `:id/modify` | `orders/$id/modify` |
| 卖家订单 | `:aggregateOrderId/seller-orders/:id` | `orders/$aggregateOrderId/seller-orders/$sellerOrderId` |

### 5.2 逐一模块迁移状态核对

#### 📊 Insights / Dashboard

| 功能 | 旧路由 (Angular) | 新路由 (React) | 迁移状态 |
|------|------------------|----------------|----------|
| 仪表板首页 | `/dashboard` | `/` | ✅ 已迁移 |
| 指标小组件 | 内置 Widget | `MetricsWidget` | ✅ 已迁移 |
| 最近订单组件 | 内置 Widget | `LatestOrdersWidget` | ✅ 已迁移 |
| 订单摘要组件 | 内置 Widget | `OrdersSummaryWidget` | ✅ 已迁移 |

#### 📦 Catalog

| 功能 | 旧路由 (Angular) | 新路由 (React) | 迁移状态 |
|------|------------------|----------------|----------|
| 产品列表 | `products` | `products` | ✅ 已迁移 |
| 产品详情 | `products/:id` | `products/$id` | ✅ 已迁移 |
| 管理变体 | `products/:id/manage-variants` | `products/$id/variants` | ✅ 已迁移 |
| 变体详情 | `products/:productId/variants/:id` | `product-variants/$id` | ✅ 已迁移 |
| 产品选项 | `products/:id/options` | `option-groups` (独立路由) | ✅ 已迁移 |
| 选项组列表 | — (内嵌在产品详情) | `option-groups` | ✅ 新增独立页面 |
| 选项组详情 | — | `option-groups/$id` | ✅ 新增 |
| Facet 列表 | `facets` | `facets` | ✅ 已迁移 |
| Facet 详情 | `facets/:id` | `facets/$id` | ✅ 已迁移 |
| Facet 值详情 | — | `facets/$facetId/values/$id` | ✅ 新增 |
| 集合列表 | `collections` | `collections` | ✅ 已迁移 |
| 集合详情 | `collections/:id` | `collections/$id` | ✅ 已迁移 |
| 资产列表 | `assets` | `assets` | ✅ 已迁移 |
| 资产详情 | `assets/:id` | `assets/$id` | ✅ 已迁移 |
| 库存 | `inventory` (→ 重定向到 products) | `product-variants` | ✅ 独立路由 |

#### 🛒 Sales

| 功能 | 旧路由 (Angular) | 新路由 (React) | 迁移状态 |
|------|------------------|----------------|----------|
| 订单列表 | `` (空路径) | `orders` | ✅ 已迁移 |
| 订单详情 | `:id` | `orders/$id` | ✅ 已迁移 |
| 草稿订单详情 | `draft/:id` | `orders/draft/$id` | ✅ 已迁移 |
| 修改订单 | `:id/modify` | `orders/$id/modify` | ✅ 已迁移 |
| 卖家订单 | `:aggregateOrderId/seller-orders/:id` | `orders/$aggregateOrderId/seller-orders/$sellerOrderId` | ✅ 已迁移 |

#### 👥 Customers

| 功能 | 旧路由 (Angular) | 新路由 (React) | 迁移状态 |
|------|------------------|----------------|----------|
| 客户列表 | `customers` | `customers` | ✅ 已迁移 |
| 客户详情 | `customers/:id` | `customers/$id` | ✅ 已迁移 |
| 客户组列表 | `groups` | `customer-groups` | ✅ 已迁移 |
| 客户组详情 | `groups/:id` | `customer-groups/$id` | ✅ 已迁移 |

#### 🏷️ Marketing

| 功能 | 旧路由 (Angular) | 新路由 (React) | 迁移状态 |
|------|------------------|----------------|----------|
| 促销列表 | `promotions` | `promotions` | ✅ 已迁移 |
| 促销详情 | `promotions/:id` | `promotions/$id` | ✅ 已迁移 |

#### ⚙️ Settings

| 功能 | 旧路由 (Angular) | 新路由 (React) | 迁移状态 |
|------|------------------|----------------|----------|
| 个人资料 | `profile` | `profile` | ✅ 已迁移 |
| 管理员列表 | `administrators` | `administrators` | ✅ 已迁移 |
| 管理员详情 | `administrators/:id` | `administrators/$id` | ✅ 已迁移 |
| 渠道列表 | `channels` | `channels` | ✅ 已迁移 |
| 渠道详情 | `channels/:id` | `channels/$id` | ✅ 已迁移 |
| 库存位置列表 | `stock-locations` | `stock-locations` | ✅ 已迁移 |
| 库存位置详情 | `stock-locations/:id` | `stock-locations/$id` | ✅ 已迁移 |
| 卖家列表 | `sellers` | `sellers` | ✅ 已迁移 |
| 卖家详情 | `sellers/:id` | `sellers/$id` | ✅ 已迁移 |
| 角色列表 | `roles` | `roles` | ✅ 已迁移 |
| 角色详情 | `roles/:id` | `roles/$id` | ✅ 已迁移 |
| 税分类列表 | `tax-categories` | `tax-categories` | ✅ 已迁移 |
| 税分类详情 | `tax-categories/:id` | `tax-categories/$id` | ✅ 已迁移 |
| 税率列表 | `tax-rates` | `tax-rates` | ✅ 已迁移 |
| 税率详情 | `tax-rates/:id` | `tax-rates/$id` | ✅ 已迁移 |
| 国家列表 | `countries` | `countries` | ✅ 已迁移 |
| 国家详情 | `countries/:id` | `countries/$id` | ✅ 已迁移 |
| 区域列表 | `zones` | `zones` | ✅ 已迁移 |
| 区域详情 | `zones/:id` | `zones/$id` | ✅ 已迁移 |
| 配送方式列表 | `shipping-methods` | `shipping-methods` | ✅ 已迁移 |
| 配送方式详情 | `shipping-methods/:id` | `shipping-methods/$id` | ✅ 已迁移 |
| 支付方式列表 | `payment-methods` | `payment-methods` | ✅ 已迁移 |
| 支付方式详情 | `payment-methods/:id` | `payment-methods/$id` | ✅ 已迁移 |
| 全局设置 | `global-settings` | `global-settings` | ✅ 已迁移 |

#### 🔧 System

| 功能 | 旧路由 (Angular) | 新路由 (React) | 迁移状态 |
|------|------------------|----------------|----------|
| 作业队列 | `jobs` | `job-queue` | ✅ 已迁移 |
| 系统状态 | `system-status` | — | ⚠️ 未迁移 |
| 定时任务 | `scheduled-tasks` | `scheduled-tasks` | ✅ 已迁移 |
| 设置存储 | — | `settings-store` | ✅ 新增 |
| API Keys | — | `api-keys` | ✅ 新增 |

#### 🔑 Authentication

| 功能 | 旧路由 (Angular) | 新路由 (React) | 迁移状态 |
|------|------------------|----------------|----------|
| 登录页 | `/login` | `/login` | ✅ 已迁移 |

### 5.3 未迁移功能汇总

| 旧路由 | 功能描述 | 建议 |
|--------|----------|------|
| `/system/system-status` | 健康检查/系统状态页面 | 在新 Dashboard 中通过扩展注册自定义路由，或跳转到旧管理后台 |

### 5.4 新增功能（旧系统没有）

| 新路由 | 功能描述 | 备注 |
|--------|----------|------|
| `product-variants` | 独立的产品变体列表页 | 旧系统中变体管理嵌套在产品详情中 |
| `option-groups` | 独立的选项组管理 | 旧系统中选项管理嵌套在产品详情中 |
| `settings-store` | 设置存储页面 | 新 Dashboard 独有 |
| `api-keys` | API Keys 管理 | 新 Dashboard 独有 |

---

## 六、实施建议

### 6.1 短期（过渡期）：双系统并行

1. **同时注册两个插件**

```typescript
// vendure-config.ts
import { AdminUiPlugin } from '@vendure/admin-ui-plugin';
import { DashboardPlugin } from '@vendure/dashboard/plugin';

const config: VendureConfig = {
    plugins: [
        AdminUiPlugin.init({ route: 'admin', port: 3002 }),
        DashboardPlugin.init({ route: 'dashboard', appDir: './dist/dashboard' }),
    ],
};
```

2. **在新 Dashboard 添加跳转入口**（针对 `system-status` 等未迁移功能）

3. **改造 `nav-main.tsx` 支持外部链接**

### 6.2 中期（迁移完成）：设置重定向

1. 在旧管理后台路由中添加重定向到新 Dashboard 对应页面
2. 使用 Nginx 重写规则将 `/admin/` 路径重定向到 `/dashboard/`

### 6.3 长期（2026 年 7 月后）：下线旧系统

1. 移除 `AdminUiPlugin`
2. 将 `/admin/` 路径全部重定向到 `/dashboard/`
3. 迁移所有第三方插件扩展到新 Dashboard API

---

## 七、关键文件索引

| 功能 | 文件路径 | 关键行号 |
|------|----------|----------|
| 旧 Admin UI 插件（含弃用声明） | `packages/admin-ui-plugin/src/plugin.ts` | 95-99 |
| 旧 Admin UI 路由守卫 | `packages/admin-ui/src/lib/core/src/providers/guard/auth.guard.ts` | 16-36 |
| 旧 Admin UI 权限服务 | `packages/admin-ui/src/lib/core/src/providers/permissions/permissions.service.ts` | 13-41 |
| 旧 Admin UI 导航构建器 | `packages/admin-ui/src/lib/core/src/providers/nav-builder/nav-builder.service.ts` | 25-207 |
| 旧 Admin UI 导航类型 | `packages/admin-ui/src/lib/core/src/providers/nav-builder/nav-builder-types.ts` | 37-81 |
| 旧 Admin UI Catalog 路由 | `packages/admin-ui/src/lib/catalog/src/catalog.routes.ts` | 16-136 |
| 旧 Admin UI Order 路由 | `packages/admin-ui/src/lib/order/src/order.routes.ts` | 6-56 |
| 旧 Admin UI Settings 路由 | `packages/admin-ui/src/lib/settings/src/settings.routes.ts` | 13-239 |
| 新 Dashboard 认证路由守卫 | `packages/dashboard/src/app/routes/_authenticated.tsx` | 7-22 |
| 新 Dashboard 权限 Hook | `packages/dashboard/src/lib/hooks/use-permissions.ts` | 23-47 |
| 新 Dashboard 权限守卫 | `packages/dashboard/src/lib/components/shared/permission-guard.tsx` | 44-51 |
| 新 Dashboard 导航菜单渲染 | `packages/dashboard/src/lib/components/layout/nav-main.tsx` | 94-350 |
| 新 Dashboard 默认菜单配置 | `packages/dashboard/src/lib/framework/defaults.ts` | 11-286 |
| 新 Dashboard 认证上下文 | `packages/dashboard/src/lib/providers/auth.tsx` | 107-255 |
| 新 Dashboard 服务端插件 | `packages/dashboard/plugin/dashboard.plugin.ts` | 119-396 |

---

**报告生成时间**：2026-05-31  
**分析代码版本**：Vendure v3.x  
**分析范围**：packages/admin-ui-plugin、packages/admin-ui、packages/dashboard
