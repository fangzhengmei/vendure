# 订单状态机与支付交互概念报告

## 1. 核心概念与架构概览

Vendure 采用**双状态机**架构来管理订单与支付的生命周期：
- **订单状态机**：管理订单的业务生命周期（购物车 → 支付 → 履约 → 完成）
- **支付状态机**：管理单笔支付的处理生命周期（创建 → 授权 → 结算 → 完成/失败）

两个状态机通过**事件驱动 + 自动迁移**的方式协同工作，支付状态的变更会自动触发订单状态的相应调整。

---

## 2. 订单状态机详解

### 2.1 订单状态定义

| 状态 | 说明 |
|------|------|
| `Created` | 订单刚创建 |
| `Draft` | 草稿订单（管理员创建） |
| `AddingItems` | 购物车状态，用户正在添加商品 |
| `ArrangingPayment` | 待支付，用户准备付款 |
| `PaymentAuthorized` | 支付已授权（两步支付） |
| `PaymentSettled` | 支付已结算（全款到账） |
| `PartiallyShipped` | 部分发货 |
| `Shipped` | 全部发货 |
| `PartiallyDelivered` | 部分送达 |
| `Delivered` | 全部送达 |
| `Modifying` | 订单修改中 |
| `ArrangingAdditionalPayment` | 待补充支付（订单修改后金额增加） |
| `Cancelled` | 已取消 |

### 2.2 关键状态迁移规则

核心迁移定义在 `default-order-process.ts:174-236`

```
Created → AddingItems / Draft
AddingItems → ArrangingPayment / Cancelled
ArrangingPayment → PaymentAuthorized / PaymentSettled / AddingItems / Cancelled
PaymentAuthorized → PaymentSettled / Cancelled / Modifying / ArrangingAdditionalPayment
PaymentSettled → PartiallyShipped / Shipped / PartiallyDelivered / Delivered / Cancelled / Modifying / ArrangingAdditionalPayment
Modifying → PaymentAuthorized / PaymentSettled / PartiallyShipped / Shipped / PartiallyDelivered / ArrangingAdditionalPayment
ArrangingAdditionalPayment → PaymentAuthorized / PaymentSettled / PartiallyShipped / Shipped / PartiallyDelivered / Cancelled
Cancelled → (无)
```

### 2.3 迁移前置条件（Guard）

在 `onTransitionStart` 钩子中进行检查：

| 目标状态 | 前置条件 |
|----------|----------|
| `ArrangingPayment` | 订单非空、有客户、有配送方式、库存充足 |
| `PaymentAuthorized` | 至少 1 笔 `Authorized` 支付，且 `Authorized + Settled` 支付总额 ≥ 订单总额 |
| `PaymentSettled` | `Settled` 状态支付总额 ≥ 订单总额 |
| `Cancelled` | 所有订单项已取消（非购物车状态下） |
| `ArrangingAdditionalPayment` | 存在未结算的订单修改（金额增加） |

---

## 3. 支付状态机详解

### 3.1 支付状态定义

| 状态 | 说明 |
|------|------|
| `Created` | 支付记录刚创建 |
| `Authorized` | 支付已授权（资金冻结，未结算） |
| `Settled` | 支付已结算（资金到账） |
| `Declined` | 支付被拒绝 |
| `Error` | 支付处理出错 |
| `Cancelled` | 支付已取消 |

### 3.2 支付状态迁移

核心迁移定义在 `default-payment-process.ts:27-46`

```
Created → Authorized / Settled / Declined / Error / Cancelled
Authorized → Settled / Error / Cancelled
Settled → Cancelled
Declined → Cancelled
Error → Cancelled
Cancelled → (无)
```

---

## 4. 事务边界与交互契约

### 4.1 核心交互机制：支付驱动订单

支付状态机的 `onTransitionEnd` 钩子是连接两个状态机的关键桥梁。每次支付状态变更后，系统会自动检查订单支付覆盖情况，并触发订单状态迁移。

**⚠️ 代码原文**：`default-payment-process.ts:77-89`

```typescript
// ⚠️ 注意：代码明确排除了 ArrangingAdditionalPayment 状态！
if (
    orderTotalIsCovered(order, 'Settled') &&
    order.state !== 'PaymentSettled' &&
    order.state !== 'ArrangingAdditionalPayment'  // ← 明确排除
) {
    await orderService.transitionToState(ctx, order.id, 'PaymentSettled');
} else if (
    orderTotalIsCovered(order, ['Authorized', 'Settled']) &&
    order.state !== 'PaymentAuthorized' &&
    order.state !== 'ArrangingAdditionalPayment'  // ← 明确排除
) {
    await orderService.transitionToState(ctx, order.id, 'PaymentAuthorized');
}
```

**关键结论**：`ArrangingAdditionalPayment` 状态下，即使支付足额，**也不会被自动推进**到 `PaymentAuthorized` 或 `PaymentSettled`。

### 4.2 事务原子性保证

**支付状态迁移的原子性**：`payment.service.ts:227-261`

```
支付状态迁移流程（同一事务）：
1. 调用 paymentStateMachine.transition() → 执行 FSM 状态迁移
2. 调用 onTransitionStart 钩子（所有 PaymentProcess + PaymentMethodHandler）
3. 保存 Payment 到数据库
4. 发布 PaymentStateTransitionEvent
5. 调用 onTransitionEnd 钩子
   → 其中包括 default-payment-process.ts 中的订单状态自动迁移逻辑
6. 提交事务
```

**⚠️ 事务语义**：支付状态保存、onTransitionEnd 钩子（包括订单状态迁移）都在**同一数据库事务**中执行。
- 如果订单状态迁移失败（如 Guard 拦截、数据库异常），**整个事务回滚**
- 支付状态也会回滚到迁移前的状态
- 不存在"支付已落库但订单迁移失败"的情况

### 4.3 唯一的风险边界：createPayment 调用外部网关

**临界区分析**：`payment.service.ts:123-162`

```
createPayment 执行流程：
1. 调用 handler.createPayment() → 调用支付网关（⚠️ 事务外执行！）
2. 进入 withTransaction 事务
3. 保存 Payment 到数据库
4. 执行支付状态机迁移
5. 保存 Payment
6. 关联 Payment 到 Order
7. 发布事件
8. 调用 onTransitionEnd 钩子（包括订单自动迁移）
9. 提交事务
```

**风险来源**：
- `handler.createPayment()` 调用第三方网关是在事务外部执行
- 这是故意的设计选择：第三方网络调用无法参与本地事务回滚
- 如果网关扣款成功但后续数据库写入失败，**会产生孤儿扣款**
- 这是整个流程中唯一可能产生不一致的地方

### 4.4 支付覆盖计算规则

`order-utils.ts:20-43`

- **有效支付**：排除 `Error`、`Declined`、`Cancelled` 状态的支付
- **支付净额** = 支付金额 - 已结算退款金额
- **足额判定**：有效支付净额总和 ≥ 订单 `totalWithTax`

---

## 5. 支付插件侧完整契约

### 5.1 PaymentMethodHandler 四大核心方法

支付插件必须实现 `PaymentMethodHandler`，定义在 `payment-method-handler.ts:354-469`

---

#### 5.1.1 createPayment — 创建支付

**调用时机**：用户在前端点击支付，调用 `addPaymentToOrder` mutation 时

**输入参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| `ctx` | `RequestContext` | 请求上下文，包含当前用户、渠道、语言等信息 |
| `order` | `Order` | 关联的订单实体，包含商品、金额、客户信息 |
| `amount` | `number` | 本次应支付金额（单位：分）， = 订单总额 - 已支付金额 |
| `args` | `ConfigArgValues<T>` | 支付方法配置参数（如 API Key、商户号等） |
| `metadata` | `PaymentMetadata` | 前端传入的元数据（如支付令牌、用户选择的支付渠道等） |
| `method` | `PaymentMethod` | 当前使用的支付方法实体 |

**返回值**：`CreatePaymentResult | CreatePaymentErrorResult`

```typescript
interface CreatePaymentResult {
    amount: number;           // 实际支付金额，必须等于传入的 amount
    state: PaymentState;      // 目标状态，必须是可从 Created 迁移的状态
    transactionId?: string;   // 支付网关返回的交易号
    errorMessage?: string;    // 错误信息（失败时）
    metadata?: {              // 附加数据
        public?: any;         // 前端可见部分（如跳转URL、二维码）
        [key: string]: any;   // 私有部分（网关内部字段）
    };
}
```

**状态限制**：
- `state` 必须是 `Created` 的合法目标状态：`Authorized` / `Settled` / `Declined` / `Error` / `Cancelled`
- 单步支付场景：返回 `state: 'Settled'`
- 两步支付场景：返回 `state: 'Authorized'`
- 支付失败：返回 `state: 'Declined'`（业务拒绝）或 `state: 'Error'`（系统错误）
- **禁止返回 `state: 'Created'`，因为支付创建本身就是从 Created 开始的

**⚠️ 执行上下文与风险**：
- **此方法在数据库事务外部执行**（因为涉及第三方网络调用）
- 临界区：`payment.service.ts:123-162`
- **孤儿扣款风险**：如果网关扣款成功但后续数据库写入失败，会产生孤儿扣款（本地无 Payment 记录）

---

#### 5.1.2 settlePayment — 结算支付（捕获）

**调用时机**：
- 管理员在后台点击"结算"按钮
- 或系统自动结算（根据业务配置）
- 调用 `settlePayment` mutation

**输入参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| `ctx` | `RequestContext` | 请求上下文 |
| `order` | `Order` | 关联订单 |
| `payment` | `Payment` | 待结算的支付实体（当前状态应为 `Authorized`） |
| `args` | `ConfigArgValues<T>` | 支付方法配置参数 |
| `method` | `PaymentMethod` | 支付方法实体 |

**返回值**：`SettlePaymentResult | SettlePaymentErrorResult`

```typescript
interface SettlePaymentResult {
    success: true;
    metadata?: PaymentMetadata;  // 结算成功的附加数据
}

interface SettlePaymentErrorResult {
    success: false;
    state?: Exclude<PaymentState, 'Settled'>;  // 失败后的目标状态，默认 'Error'
    errorMessage?: string;                      // 错误信息
    metadata?: PaymentMetadata;                 // 失败附加数据
}
```

**状态限制**：
- 调用此方法时，支付当前状态必须是 `Authorized`
- 成功时系统自动将支付迁移到 `Settled`
- 失败时可以指定迁移到 `Error` 或其他自定义状态（需在 PaymentProcess 中定义合法迁移）

**事务语义**：
- 支付状态迁移与订单状态迁移在同一事务中
- 如果订单状态迁移失败，支付状态也会回滚

**特殊约定**：
- 如果支付网关在 `createPayment` 时已经完成结算（单步支付），此方法只需返回 `{ success: true }` 即可

---

#### 5.1.3 cancelPayment — 取消支付

**调用时机**：
- 管理员在后台点击"取消"按钮
- 调用 `cancelPayment` mutation
- 通常用于取消已授权但未结算的支付

**输入参数**：与 `settlePayment` 相同

**返回值**：`CancelPaymentResult | CancelPaymentErrorResult | null | undefined`

```typescript
interface CancelPaymentResult {
    success: true;
    metadata?: PaymentMetadata;
}

interface CancelPaymentErrorResult {
    success: false;
    state?: Exclude<PaymentState, 'Cancelled'>;  // 失败后的目标状态，默认 'Error'
    errorMessage?: string;
    metadata?: PaymentMetadata;
}
```

**状态限制**：
- 调用此方法时，支付当前状态通常是 `Created` 或 `Authorized`
- 成功时系统自动将支付迁移到 `Cancelled`
- 如果插件不实现此方法，取消操作会直接迁移到 `Cancelled` 而不调用网关

---

#### 5.1.4 onStateTransitionStart — 状态迁移前置钩子

**调用时机**：支付状态机在执行任何状态迁移之前

**输入参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| `fromState` | `PaymentState` | 当前状态 |
| `toState` | `PaymentState` | 目标状态 |
| `data` | `PaymentTransitionData` | 包含 `ctx`、`payment`、`order` |

**返回值**：
- `true` / `void` / `undefined`：允许迁移
- `false`：禁止迁移，无错误信息
- `string`：禁止迁移，返回错误信息

**使用场景**：
- 风控检查：特定金额的支付需要额外审核
- 业务规则校验：如同一订单不允许重复支付
- 状态一致性检查：确保迁移符合支付网关侧的状态

---

### 5.2 方法调用与状态流转对应关系

| 业务操作 | 调用方法 | 支付状态流转 | 触发订单状态变更 |
|----------|----------|--------------|------------------|
| 用户发起支付 | `createPayment` | `Created → Settled` 或 `Created → Authorized` | 支付足额时自动迁移到 `PaymentSettled` 或 `PaymentAuthorized`（但 `ArrangingAdditionalPayment` 除外） |
| 管理员结算授权 | `settlePayment` | `Authorized → Settled` | 支付足额时自动迁移到 `PaymentSettled`（但 `ArrangingAdditionalPayment` 除外） |
| 管理员取消支付 | `cancelPayment` | `Authorized → Cancelled` | 支付不足额时订单可能回退 |
| 网关回调 | `transitionToState` | 任意合法迁移 | 自动检查并迁移订单状态（但 `ArrangingAdditionalPayment` 除外） |

**事务保证**：上述所有操作中，支付状态迁移与订单状态迁移都在同一事务中，要么都成功，要么都回滚。

---

## 6. 支付状态回写订单的自动推进与阻断矩阵

### 6.1 自动推进的触发逻辑

支付状态变更后，`default-payment-process.ts:77-89` 的 `onTransitionEnd` 钩子执行逻辑：

```
支付状态变更
    ↓
检查 order.state
    ├─ ▶ 是 ArrangingAdditionalPayment → ⛔ 直接跳过，不做任何事
    ├─ ▶ 已在 PaymentSettled → ⛔ 跳过（已经是目标状态）
    ├─ ▶ 已在 PaymentAuthorized → ⛔ 跳过（已经是目标状态）
    └─ ▶ 其他状态 → 继续检查
                        ↓
检查支付覆盖
    ├─ ▶ Settled 支付足额 → 尝试迁移到 PaymentSettled
    └─ ▶ Authorized + Settled 支付足额 → 尝试迁移到 PaymentAuthorized
                        ↓
订单状态机 Guard 检查
    ├─ ▶ 迁移图允许 + Guard 通过 → ✅ 事务提交，自动迁移成功
    └─ ▶ 迁移图不允许 / Guard 拦截 → ❌ 整个事务回滚，支付状态也回滚
```

**⚠️ 重要**：订单迁移失败时，**支付状态也会回滚**，不存在"支付已落库但订单迁移失败"的半成功状态。

### 6.2 自动推进矩阵（按实际代码规则）

| 订单当前状态 | 支付事件 | 是否自动推进 | 结果状态 | 说明 |
|--------------|----------|------------|----------|------|
| `ArrangingPayment` | 新增 `Settled` 支付，足额 | ✅ 是 | `PaymentSettled` | 单步支付完成 |
| `ArrangingPayment` | 新增 `Authorized` 支付，足额 | ✅ 是 | `PaymentAuthorized` | 两步支付授权完成 |
| `ArrangingPayment` | 多笔支付合计足额 | ✅ 是 | `PaymentSettled` / `PaymentAuthorized` | 分多次支付完成 |
| `PaymentAuthorized` | `Authorized` 支付变为 `Settled` | ✅ 是 | `PaymentSettled` | 授权后结算完成 |
| **`ArrangingAdditionalPayment`** | 新增补充支付，足额 | **⛔ 否** | **保持 `ArrangingAdditionalPayment`** | **代码明确排除此状态** |
| `Modifying` | 任何支付变更 | ⛔ 否 | 保持 `Modifying` | 不在自动推进列表中 |
| `PartiallyShipped` / `Shipped` / `PartiallyDelivered` / `Delivered` | 任何支付变更 | ⛔ 否 | 保持原状态 | 状态迁移图不允许 |
| `Cancelled` | 任何支付变更 | ⛔ 否 | 保持 `Cancelled` | 终态不可迁移 |

---

### 6.3 ArrangingAdditionalPayment 为什么不会被自动推进

**根本原因**：代码明确排除

`default-payment-process.ts:80` 和 `:86` 两处硬编码判断：

```typescript
order.state !== 'ArrangingAdditionalPayment'
```

**设计意图**：

`ArrangingAdditionalPayment` 是一个**受控的中间状态**，设计上要求：
1. 必须通过**手动**完成补充支付流程
2. 必须通过**手动**触发订单修改与支付的关联绑定
3. 必须通过**管理员确认所有 OrderModification.isSettled = true**
4. 防止自动推进绕过了修改结算检查

**正确的补充支付完成流程**：

```
步骤1：订单在 ArrangingAdditionalPayment
         ↓ 管理员调用 addManualPaymentToOrder
         ↓ 支付创建成功，状态变为 Settled
         ↓ 支付状态机 onTransitionEnd 触发
         ↓ 检查 order.state === 'ArrangingAdditionalPayment' → ⛔ 跳过自动迁移
         ↓
步骤2：需要手动触发迁移
         ↓ 管理员调用 transitionToState(orderId, 'PaymentSettled')
         ↓
步骤3：订单状态机 Guard 检查
         ↓ default-order-process.ts:280-297 检查：
         ↓ - 检查 deficit = 订单总额 - 已支付总额
         ↓ - 检查所有 OrderModification.isSettled = true
         ↓
步骤4：✅ 迁移成功 → PaymentSettled
```

---

### 6.4 会被 Guard 拦住的场景

即使代码规则允许，`onTransitionStart` 钩子的检查也可能拦截迁移：

#### 6.4.1 示例：`ArrangingAdditionalPayment` → `PaymentSettled` 的双重检查

`default-order-process.ts:280-297` 中有专门约束：

```typescript
if (fromState === 'ArrangingAdditionalPayment' && toState !== 'Cancelled') {
    const deficit = order.totalWithTax - totalCoveredByPayments(order);
    if (0 < deficit) {
        return 'message.cannot-transition-from-arranging-additional-payment';
    }
}
```

#### 6.4.2 订单修改 + 补充支付的完整约束链

```
步骤1：管理员发起修改
  PaymentSettled → Modifying
  （必须通过 transitionToState 手动触发）

步骤2：执行 modifyOrder
  在 Modifying 状态下调用 modifyOrder mutation
  系统计算价格变更 delta = 新总额 - 原总额

步骤3：根据 delta 决定去向
  ▶ delta < 0：需要退款 → 调用 refundOrder
  ▶ delta = 0：无金额变化 → 可直接迁回 PaymentSettled
  ▶ delta > 0：需要补充支付
     → 订单必须先迁移到 ArrangingAdditionalPayment
        （从 Modifying 迁出时自动检查）
     → 检查点1：Modifying → ArrangingAdditionalPayment 迁移
        default-order-process.ts:262-277 检查：
        - 如果存在未结算的 OrderModification（isSettled = false）
        - 必须先迁到 ArrangingAdditionalPayment
        - 禁止直接迁回 PaymentSettled

步骤4：在 ArrangingAdditionalPayment 状态下
  调用 addManualPaymentToOrder 添加补充支付
  支付状态变为 Settled
  ↓
  ⚠️ 支付状态机 onTransitionEnd 触发
  ↓ 检查 order.state === 'ArrangingAdditionalPayment'
  ↓ ⛔ 跳过自动迁移！
  ↓
  必须手动调用 transitionToState 到 PaymentSettled

步骤5：手动迁回 PaymentSettled
  尝试 ArrangingAdditionalPayment → PaymentSettled
  → 检查点2：default-order-process.ts:280-297
     - 确认 deficit = 0
     - 所有 OrderModification.isSettled = true
  → ✅ 迁移成功
```

**关键理解**：
- `Modifying` 状态不允许直接添加支付
- 必须先迁到 `ArrangingAdditionalPayment` 才能接受新支付
- `ArrangingAdditionalPayment` 下支付成功后**不会自动推进**，必须手动触发
- 双重检查机制确保订单修改与支付严格对应，防止金额不一致

---

### 6.5 不会自动推进的状态汇总

| 订单状态 | 说明 | 手动干预方式 |
|----------|------|--------------|
| `Modifying` | 修改进行中 | 完成修改后手动迁移 |
| `ArrangingAdditionalPayment` | 待补充支付 | 需调用 `addManualPaymentToOrder` + 手动 transitionToState |
| `PartiallyShipped` / `Shipped` / `PartiallyDelivered` / `Delivered` | 履约中 | 支付只影响财务状态，不回退履约 |
| `Cancelled` | 已取消 | 终态不可迁移 |

---

## 7. 典型交互流程

### 7.1 流程一：单步支付（即时结算）

```
用户操作：
  [添加商品] → [去结算] → [确认支付]

状态变迁：
  AddingItems
       ↓ (用户点击结算)
  ArrangingPayment
       ↓ (调用 addPaymentToOrder)
  创建 Payment: Created → Settled
       ↓ (支付状态机 onTransitionEnd 自动触发)
  PaymentSettled
       ↓ (后续履约流程)
  PartiallyShipped → Shipped → PartiallyDelivered → Delivered
```

**关键节点**：
1. `ArrangingPayment` 是支付入口状态
2. 支付创建成功后立即变为 `Settled`
3. 支付状态机自动检测并将订单迁移到 `PaymentSettled`
4. **事务保证**：支付状态与订单状态迁移在同一事务中

### 7.2 流程二：两步支付（授权 + 结算）

```
用户操作：
  [添加商品] → [去结算] → [预授权]

状态变迁：
  AddingItems → ArrangingPayment
       ↓ (创建支付)
  Payment: Created → Authorized
       ↓ (自动触发)
  PaymentAuthorized
       ↓ (管理员点击结算 / 自动结算)
  Payment: Authorized → Settled
       ↓ (自动触发)
  PaymentSettled
```

**适用场景**：酒店预授权、高价值商品风控等

### 7.3 流程三：订单修改 + 补充支付（⚠️ 完全手动流程）

```
已完成订单：
  PaymentSettled
       ↓ (管理员修改订单，增加商品)
  Modifying
       ↓ (modifyOrder 计算 delta > 0)
  系统要求迁移到 ArrangingAdditionalPayment
       ↓ (调用 addManualPaymentToOrder)
  创建新 Payment: Created → Settled
       ↓ (支付状态机 onTransitionEnd 触发
       ↓ ⚠️ 检查到 ArrangingAdditionalPayment → 跳过自动迁移
       ↓
  必须手动调用 transitionToState(orderId, 'PaymentSettled')
       ↓ (Guard 检查所有修改已结算)
  PaymentSettled
```

**关键约束**：
- 只能从 `PaymentAuthorized` 或 `PaymentSettled` 进入 `Modifying`
- 修改后金额增加 → 必须进入 `ArrangingAdditionalPayment` 补充支付
- **补充支付成功后不会自动推进，必须手动触发迁移**
- 手动迁移时 Guard 检查所有 OrderModification 已结算

---

## 8. 失败场景与口径收敛

### 8.1 唯一的不一致场景：createPayment 事务外扣款成功但本地落库失败（孤儿扣款）

**这是整个流程中唯一可能产生不一致的场景**。

**场景描述**：
- 支付网关扣款成功（`handler.createPayment()` 在事务外调用）
- 但后续数据库写入失败（事务回滚）
- 本地 **没有** Payment 记录
- 订单仍停留在 `ArrangingPayment`
- 用户看到"支付失败"，但银行卡已扣款

**判定依据**：
- 本地 Payment 表中 **找不到** 该 `transactionId` 的记录
- 支付网关后台显示交易成功
- 订单状态仍为 `ArrangingPayment`

**风险根源**：
- `handler.createPayment()` 调用第三方网关是在事务外部执行
- 这是故意的设计选择：第三方网络调用无法参与本地事务回滚
- 临界区：`payment.service.ts:123-162`

---

### 8.2 其他所有失败场景：事务保证一致

除了上述孤儿扣款场景，**所有其他失败场景都受事务保护**，不会产生不一致。

#### 场景 A：支付网关返回失败

```
调用 handler.createPayment() → 网关返回失败（余额不足、风控拦截等）
    ↓
不进入数据库事务
    ↓
返回错误给前端
    ↓
结果：一致，无 Payment 记录，订单保持 ArrangingPayment
```

#### 场景 B：Guard 拦截订单迁移

```
支付状态变更触发 onTransitionEnd
    ↓
尝试订单状态迁移 → Guard 拦截（如库存不足、状态迁移图不允许）
    ↓
抛出异常 → 整个事务回滚
    ↓
支付状态也回滚到迁移前的状态
    ↓
结果：一致，支付状态与订单状态都未变更
```

#### 场景 C：数据库异常

```
支付状态变更触发 onTransitionEnd
    ↓
执行订单状态迁移 → 数据库异常（宕机、死锁等）
    ↓
事务回滚
    ↓
支付状态也回滚到迁移前的状态
    ↓
结果：一致，支付状态与订单状态都未变更
```

#### 场景 D：补充支付成功但未自动推进

```
ArrangingAdditionalPayment
     ↓ (调用 addManualPaymentToOrder)
支付创建成功：Created → Settled
     ↓
支付状态机 onTransitionEnd 触发
     ↓
⚠️ 检查到 ArrangingAdditionalPayment → 跳过自动迁移
     ↓
结果：一致，支付状态已更新为 Settled，但订单状态保持 ArrangingAdditionalPayment
     这是设计行为，需要管理员手动触发订单迁移
```

**关键结论**：除了 `handler.createPayment()` 事务外执行导致的孤儿扣款场景，**所有其他失败都受事务保护，要么全成功，要么全回滚**，不存在"支付已落库但订单迁移失败"的半成功状态。

---

## 9. 孤儿扣款场景：补偿策略与排查顺序

### 9.1 幂等键设计

为了支持对账和补偿，必须正确设计幂等键：

| 幂等键 | 说明 | 用途 |
|--------|------|------|
| **订单号 + 支付方式码** | 组合键 | 防止同一订单重复支付 |
| **支付网关 transactionId** | 网关返回的唯一交易号 | 用于 Webhook 回调匹配（**必须返回**） |
| **客户端请求唯一标识** | 如 UUID | 防止重复提交 |

**插件实现要求**：

```typescript
// createPayment(ctx, order, amount, args, metadata, method) {
    // 1. 生成或获取幂等键
    const idempotencyKey = metadata?.idempotencyKey || generateUUID();

    // 2. 调用支付网关时传入幂等键
    const result = await gateway.createCharge({
        orderId: order.id,
        amount,
        idempotencyKey,
        // ...
    });

    // 3. 必须返回 transactionId
    return {
        amount,
        state: 'Settled',
        transactionId: result.transactionId,  // ⚠️ 必须返回
        metadata: {
            gatewayTransactionId: result.transactionId,
            idempotencyKey,
            // ...
        }
    };
}
```

---

### 9.2 补偿策略（按优先级排序）

#### 优先级 1：支付网关回调 / Webhook（最佳方案）

**原理**：大多数支付网关在交易状态变更时会发送异步回调。即使本地创建支付失败，回调仍会到达。

**处理流程**：
```
1. 支付网关发送回调，携带 `transactionId`
2. 本地系统根据 `transactionId` 查询 Payment 记录
3. 未找到记录 → 这就是孤儿扣款场景
4. 补偿动作：
   - 根据回调中的订单号查找订单
   - 验证订单状态是否为 ArrangingPayment
   - 验证金额是否匹配
   - 调用 `addPaymentToOrder` 或直接创建 Payment 记录
   - 将支付状态设为 `Settled`
   - 系统自动触发订单状态迁移到 `PaymentSettled`
   - 通知用户支付成功
```

**关键依赖**：
- 插件在 `createPayment` 时必须将 `transactionId` 返回（即使本地失败，网关侧已有此 ID）
- Webhook 必须做幂等处理（同一 `transactionId` 多次回调只处理一次）

---

#### 优先级 2：定时轮询 + 对账

如果支付网关不提供 Webhook，或 Webhook 也丢失，需要定时轮询。

**实现方式**：
```
1. 插件实现定时任务（如每 5 分钟）
2. 查询最近 1 小时内状态为 `ArrangingPayment` 的订单
3. 对每个订单，调用支付网关查询接口，传入订单号
4. 如果网关返回该订单已有成功支付
5. 补偿动作：同 Webhook 处理流程
```

**优化建议**：
- 只轮询最近一段时间的订单（如 24 小时内）
- 设置最大轮询次数，避免无限轮询
- 轮询发现的异常记录到专门的对账表，供人工审核

---

#### 优先级 3：用户主动刷新时触发查询

当用户回到订单详情页时，前端可以触发一次支付状态查询。

**流程**：
```
1. 用户打开"我的订单"，看到订单状态为"待支付"
2. 系统在后台静默调用支付网关查询接口
3. 如果网关返回支付成功
4. 补偿动作：自动创建本地支付记录并迁移订单状态
5. 前端实时更新状态为"已支付"
```

---

#### 优先级 4：人工对账兜底

如果以上自动化方式都失败，最终依赖人工对账。

**触发场景**：
- 用户投诉："我付了钱但订单显示未支付"
- 定期财务对账发现差异

**处理流程**：
```
1. 客服收集用户支付凭证（截图、交易号）
2. 运维/财务在支付网关后台查询该交易
3. 确认网关侧交易成功，本地无记录
4. 补偿动作：
   - 管理员在后台调用 `addManualPaymentToOrder`
   - 填写正确的 `transactionId` 和金额
   - 手动标记支付为 `Settled`
   - 手动 transitionToState 到 `PaymentSettled`
```

---

### 9.3 标准排查顺序（SOP）

当收到"扣款成功但订单未更新"的投诉时，按以下顺序排查：

```
步骤 1：检查支付网关侧状态
  ▶ 登录支付网关后台，用订单号 / 交易号查询
  ▶ 确认网关侧交易状态是否为"成功"
  ▶ 记录网关返回的详细信息（交易时间、金额、支付方式）

步骤 2：检查本地 Payment 表（核心区分点）
  ▶ SELECT * FROM payment WHERE transactionId = 'xxx';
  ├─ ▶ 有记录 → 不是孤儿扣款，走其他排查路径
  │     ▶ 检查支付状态是什么
  │     ▶ 检查订单状态与支付状态是否匹配
  │     ▶ 检查 OrderHistory 是否有相关记录
  └─ ▶ 无记录 → 孤儿扣款场景，必须走对账补偿流程
        ▶ 检查 Webhook 日志
        ▶ 检查是否有轮询记录
        ▶ 走对账补偿流程

步骤 3（孤儿扣款场景）：检查本地 Webhook 日志
  ▶ 搜索日志中是否有该交易号的回调记录
  ▶ 检查回调处理是否成功（状态码、错误信息）
  ▶ 如果回调失败，查看失败原因：
    - 验签失败？ → 检查密钥配置
    - 数据库错误？ → 检查数据库连接
    - 订单不存在？ → 检查订单是否被删除 / 订单号不匹配？

步骤 4：检查订单状态与历史
  ▶ 查看订单当前状态
  ▶ 查看 OrderHistory 中是否有支付相关记录
  ▶ 检查 OrderModification 是否有未结算的修改

步骤 5：执行补偿
  ▶ 孤儿扣款：按对账补偿策略处理
  ▶ 其他场景：根据具体原因处理
  ▶ 记录补偿操作日志，便于后续审计
  ▶ 通知用户支付成功

步骤 6：根因分析与预防
  ▶ 数据库宕机？ → 检查数据库高可用配置
  ▶ 网络中断？ → 检查网络稳定性
  ▶ Webhook 丢失？ → 增加 Webhook 重试机制
  ▶ 插件未返回 transactionId？ → 修复插件代码
```

---

## 10. 失败场景汇总

### 10.1 场景一：支付创建失败（网关返回错误）

```
ArrangingPayment
     ↓ (调用 addPaymentToOrder)
  调用支付网关 → 失败（网络错误、余额不足等）
     ↓
  不进入数据库事务
     ↓
  订单状态保持 ArrangingPayment
     ↓
  用户可选择：重试支付 / 更换支付方式 / 取消订单
```

**补偿方式**：
- 前端展示错误信息，允许用户重试
- 无 Payment 记录，不影响后续支付
- 订单始终停留在 `ArrangingPayment`，可接受新的支付尝试

### 10.2 场景二：支付网关回调延迟 / 丢失

```
用户在支付网关完成支付
     ↓
  网关回调延迟 / 网络中断
     ↓
  订单停留在 ArrangingPayment
     ↓
  如果本地 Payment 已落库 → 等待下次状态变更时自动重试
  如果本地 Payment 未落库 → 孤儿扣款场景，需要对账补偿
```

**补偿方式**：
1. **定时轮询**：支付插件实现轮询机制，主动查询支付状态
2. **用户主动刷新**：用户回到订单页时触发状态同步
3. **手动查询**：管理员在后台触发支付状态查询

### 10.3 场景三：支付结算失败（两步支付）

```
PaymentAuthorized
     ↓ (调用 settlePayment)
  调用支付网关结算 → 失败（授权过期、风控拦截等）
     ↓
  Payment: Authorized → Error
     ↓
  订单状态保持 PaymentAuthorized
     ↓
  管理员介入：重新结算 / 取消订单
```

### 10.4 场景四：Guard 拦截或数据库异常（事务回滚）

```
支付状态变更触发 onTransitionEnd
     ↓
  尝试订单状态迁移 → 失败（Guard 拦截 / 数据库异常）
     ↓
  抛出异常 → 整个事务回滚
     ↓
  支付状态也回滚到迁移前的状态
     ↓
  订单状态保持原状态
```

**补偿方式**：
- 这是"要么全成功，要么全回滚"的事务保证
- 支付状态没有落库（或已回滚），需要重新发起支付
- 下次支付成功时会重新尝试迁移
- 管理员也可手动触发支付状态迁移

### 10.5 场景五：补充支付成功但订单未自动推进

```
ArrangingAdditionalPayment
     ↓ (调用 addManualPaymentToOrder)
  支付创建成功：Created → Settled
     ↓
  支付状态机 onTransitionEnd 触发
     ↓
  ⚠️ 检查到 ArrangingAdditionalPayment → 跳过自动迁移
     ↓
  订单保持 ArrangingAdditionalPayment
```

**补偿方式**：
- 这是设计行为，不是 bug
- 管理员需要手动调用 `transitionToState` 到 `PaymentSettled`
- 确保所有 OrderModification.isSettled = true

### 10.6 场景六：孤儿扣款（最严重，唯一的不一致）

```
调用 handler.createPayment() → ✓ 网关扣款成功（事务外执行）
     ↓
  进入 withTransaction 事务
     ↓
  保存 Payment 到数据库 → ✗ 失败（数据库宕机 / 网络中断）
     ↓
  事务回滚
     ↓
  结果：本地无 Payment 记录，用户钱已被扣
```

**补偿方式**：
- 这是唯一的不一致场景，必须通过对账补偿
- 优先级：Webhook 回调 → 定时轮询 → 用户刷新 → 人工对账
- 不存在"自动最终一致"，必须主动补偿

---

## 11. 关键设计原则

### 11.1 原子性保障

- 支付状态保存与 onTransitionEnd 钩子在同一数据库事务中（`payment.service.ts:241-261`）
- 订单状态迁移同样是事务性的
- 除了 `handler.createPayment()` 调用外部网关，**所有操作都是原子的**
- **唯一例外**：`handler.createPayment()` 调用第三方网关是在事务外部，这是孤儿扣款风险的唯一来源

### 11.2 事务一致性的边界

**适用场景（所有内部操作）**：
- 支付状态迁移与订单状态迁移在同一事务中
- 要么全成功，要么全回滚
- 不存在"支付已落库但订单迁移失败"的半成功状态
- Guard 拦截或数据库异常都会导致整个事务回滚

**不适用场景（孤儿扣款）**：
- `handler.createPayment()` 调用外部网关在事务外执行
- 网关扣款成功但本地落库失败 = 孤儿扣款（真正的不一致）
- 必须通过 Webhook + 轮询 + 人工对账四层机制主动补偿

### 11.3 可扩展性

- 通过 `OrderProcess` 可自定义订单状态和迁移规则
- 通过 `PaymentProcess` 可自定义支付流程（如增加风控审核状态）
- 通过 `PaymentMethodHandler` 可接入任意支付网关

### 11.4 防御性设计

- 事务原子性：支付状态迁移与订单迁移要么都成功，要么都回滚
- 双重检查机制：支付状态机自动迁移 + 订单状态机 Guard 校验
- 幂等设计：同一支付的多次回调不会重复处理
- 人工兜底：所有关键节点都提供手动干预能力
- 明确排除 `ArrangingAdditionalPayment` 自动推进，防止绕过修改结算检查

---

## 12. 总结：交互闭环与风险边界

```
┌─────────────────┐      创建支付       ┌─────────────────┐
│  订单状态机      │ ─────────────────→ │  支付状态机      │
│  (Order FSM)    │  addPaymentToOrder  │  (Payment FSM)  │
└─────────────────┘                    └─────────────────┘
          │                                      │
          │                                      │ 状态变更
          │                                      ▼
          │                            ┌─────────────────┐
          │  自动检查支付覆盖           │  支付网关回调    │
          └──────────────────────────── │  (Webhook)      │
           onTransitionEnd 钩子         └─────────────────┘
                  │
                  ▼
          ┌─────────────────────────────────┐
          │  检查 order.state                 │
          │  ├─ ▶ ArrangingAdditionalPayment → ⛔ 跳过
          │  ├─ ▶ 已在目标状态 → ⛔ 跳过
          │  └─ ▶ 其他状态 → 继续检查       │
          └─────────────────────────────────┘
                  │
                  ▼
          ┌─────────────────┐
          │  Guard 校验      │
          │  - 状态迁移图    │
          │  - 前置条件      │
          │  - 修改结算检查  │
          └─────────────────┘
                  │
                  ├─ ✓ 通过 → 事务提交，订单迁移成功
                  └─ ✗ 拦截 → 事务回滚，支付状态也回滚

⚠️ 唯一风险边界：
┌─────────────────────────────────────────────────────────────┐
│  handler.createPayment() 在事务外执行                        │
│  → 网关扣款成功但本地落库失败 = 孤儿扣款（唯一不一致场景）    │
│  → 必须通过对账补偿，不存在自动最终一致                       │
└─────────────────────────────────────────────────────────────┘
```

**关键结论**：
1. **单向驱动**：订单状态机发起支付创建，支付状态机通过回调驱动订单状态迁移
2. **事务原子性**：除了 `handler.createPayment()` 外部网关调用，所有内部操作都是原子的，要么全成功，要么全回滚
3. **唯一风险**：只有 `handler.createPayment()` 事务外执行可能导致孤儿扣款，这是唯一的不一致场景
4. **多层防御**：Webhook → 轮询 → 用户刷新 → 人工对账，四层补偿机制应对孤儿扣款
5. **严格约束**：订单修改与补充支付有完整的状态约束链，`ArrangingAdditionalPayment` 排除自动迁移，防止财务不一致
6. **幂等设计**：所有外部交互必须设计幂等键，支持对账和补偿
